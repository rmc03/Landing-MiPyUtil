---
description: Amplificar un diseno seguro o planexistente a algo mas audaz
---

Load the impeccable skill using the skill tool (name: "impeccable"), then run a bolder pass on the target.

Steps:
1. Run: `node ".agents/skills/impeccable/scripts/context.mjs"`
2. Load reference/bolder.md and reference/craft-floor.md from the skill
3. Scope is sovereign: touch ONLY the named target, nothing else
4. Analyze why the section reads flat - look at what neighbors do that this section doesn't
5. Amplify using the system's own vocabulary:
   - Reuse existing motifs and type scale at full strength
   - Keep content true (preserve existing claims)
   - Commit then clarify (one decisive move, then quiet surroundings)
   - Give it its own rhythm (peak in the scroll)
6. Apply the skeleton test: does the bare structure communicate meaning without copy?
7. Verify: no new colors/fonts/primitives appeared, conventions still work, same brand only more sure

If no specific target is provided, ask the user which section to make bolder.
Target: $ARGUMENTS (the section or component to amplify)
